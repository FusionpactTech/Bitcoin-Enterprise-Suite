//! Cross-chain monitoring for CCI-SAT

use crate::{Error, Result};

/// Monitoring system
pub struct Monitor;

impl Monitor {
    /// Create a new monitor
    pub fn new() -> Self {
        Monitor
    }
}