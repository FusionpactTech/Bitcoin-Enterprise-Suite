//! Cross-chain protocols for CCI-SAT

use crate::{Error, Result};

/// Protocol implementation
pub struct Protocol;

impl Protocol {
    /// Create a new protocol
    pub fn new() -> Self {
        Protocol
    }
}